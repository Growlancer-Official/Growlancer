node_modules/
dist/
.DS_Store
*.log
coverage/
.vite/

# Local environment files must never be committed.
.env
.env.local
.env.*.local

# Keep only templates in source control.
!.env.example

.vercel
nul
supabase/supabase/
supabase/functions/test-*/
output.txt
test-base64.txt

# TypeScript incremental build artifacts (never commit).
*.tsbuildinfo

# Supabase CLI temp dir (never commit).
supabase/.temp/

# Freebuff internal worktrees (never commit).
.freebuff/

# Security reports / PDFs (never commit).
*.pdf
